
<div class="card mb-3" style="max-width: 540px;">
  <div class="row g-0">
    <div class="col-md-4">
      <img src="<%= post.user.photolink%>" alt="User Profile Pic">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h6 class="card-title"><%= link_to post.user.username, user_posts_path( post.user ) %> </h6>
        <p class="card-text"><%= post.content %></p><br>
        <p><%= link_to "Posteado #{time_ago_in_words(post.created_at)} atrás", post %> <br>
        
        <% liked = Like.find_by( post: post, user: current_user ) != nil %>
      <% btn_color = liked ? "warning" : "default" %>

      <%= link_to "cuaclike", post_likes_path( post ), method: :post, class: "btn btn-#{btn_color} btn-xs", disabled: !user_signed_in? %>
      <%= post.likes.count %><%= (post.likes.count) == 1 ? 'Like' : 'Likes'%>
        
      </div>
    </div>
  </div>
</div>

 