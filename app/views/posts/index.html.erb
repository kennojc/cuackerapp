
<% if user_signed_in? %>
  <%= render 'form' %>
<% end %>
<br>

<h1>Quacks</h1> <h6>(como tweets, pero mejores)</h6>

<% @posts.each do |post| %>
  <%= render partial: "posts/#{post.post_class}", locals: {post: post} %>
  <% end %>

  <%= paginate @posts %>
  

